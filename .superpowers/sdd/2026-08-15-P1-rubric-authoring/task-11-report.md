# 작업 11 보고

## 상태

고침 1차 구현 완료, 재검토 대기. 시작 기준은 `b89b3ca`, 최초 구현 커밋은
`cd491fa`, 고침 1차 커밋은 `956e728`이다. 최초 보고의 완료 판단은 안쪽
성취기준 입력 손실, 열린 파일 상태 오류, 성취수준 키 영역 누락이 재현되어
철회했고 아래 고침으로 바로잡았다.

## 고침 1차

- `AchievementStandard`가 안쪽 알 수 없는 칸을 거부하고, 식별값과 성취기준
  원문 및 수준 설명을 엄격한 문자열로 받는다.
- `item_range`는 정확히 두 개의 엄격한 정수만 받으며 문자열 숫자와 불 값을
  정수로 바꾸지 않는다.
- 성취수준 설명과 수준 경계값의 키를 문자열 `1`, `2`, `3`의 부분집합으로
  제한했다. 빈 사전과 일부 수준만 있는 사전은 기존 계약대로 허용한다.
- 수준 경계값은 엄격한 정수만 받는다. 같은 공용 자료형을 평가 입력과 출력,
  루브릭, `RubricCompileAuthority`에 적용하고 공용 업무 검증기도 같은 키 영역을
  확인한다.
- 만들기와 부분 수정 모두 안쪽 알 수 없는 칸, 문자열 및 불 문항 범위, 닫힌
  영역 밖 수준 키를 422로 거부한다. 정상 교사 정본은 저장 뒤 조회에서도 같은
  값으로 돌아오는 것을 확인했다.
- 파일 상태는 `os.lstat` 한 번으로만 확인한다. `FileNotFoundError`만 파일 없음으로
  처리하고 파일, 디렉터리, 심볼릭 링크, 접근 거부, 잘못된 경로와 나머지 오류는
  모두 고정 409로 막는다.
- 파일 상태 오류 뒤 평가와 문서 행 및 실제 파일이 그대로 남고, 응답에 저장
  경로나 내부 오류가 나오지 않는 회귀 시험을 추가했다.
- 작업 3, 4, 9, 11 계획 예시를 같은 엄격 자료형과 단일 파일 상태 조회 계약으로
  갱신했다.

## 실패 뒤 성공 근거

- 먼저 평가 관리 API 시험 스물네 건을 작성했다.
- 첫 초점 검사에서 모든 요청이 아직 등록되지 않은 경로의 404 응답을 받아
  예상대로 실패했다.
- 입력과 출력 스키마, 평가 라우터, 오류 경계, 앱 라우터 등록을 구현했다.
- 최종 초점 검사 스물네 건과 전체 백엔드 검사 이백삼십삼 건이 통과했다.

## 구현 내용

- 평가 만들기, 최신순 목록, 단건 조회, 부분 수정, 삭제 경로를 추가했다.
- 평가명, 과목, 학년, 총점, 성취기준, 수준 경계값을 만들기와 수정 입력에서
  모두 받고 응답에서도 같은 값으로 돌려준다.
- 평가명과 과목의 빈 문자열 및 공백 문자열, 범위 밖 학년, 영 이하 총점,
  알 수 없는 입력 칸, 빈 수정 요청, 명시적인 null 변경을 422로 거부한다.
- 기존 공용 수준 경계값 규칙을 재사용해 총점 범위 밖 경계값과 낮은 총점으로
  기존 경계값을 무효화하는 수정을 422로 거부한다.
- 없는 평가는 읽기, 수정, 삭제 모두 고정된 404 응답으로 처리한다.
- 쓰기 중 고유값 또는 외래 키를 포함한 자료 무결성 오류가 나면 세션을
  되돌리고 원래 질의문이나 드라이버 내용을 내보내지 않는 고정 409 응답으로
  바꾼다.
- 평가 삭제는 실제 파일이 없는 `SourceDocument`와 `RubricDraft` 연관 행의
  데이터베이스 연쇄 삭제를 확인했다.
- 설정 라우터를 유지하면서 현재 실제로 존재하는 평가 라우터만 앱에 누적
  등록했다. 문서 업로드와 루브릭 API는 추가하지 않았다.

## 로컬 파일 삭제 판정

작업 12 계획은 업로드 파일을 만든 뒤 문서 행을 저장하지만 평가 삭제와 파일
정리를 묶는 저장 서비스 계약을 아직 제공하지 않는다. 지금 평가 삭제에서 파일을
직접 지우면 작업 12의 저장 경계를 앞질러 구현하고, 임의의 `stored_path`를 믿어
사용자 파일을 지울 위험이 있다.

따라서 연결 문서의 로컬 경로에 파일이나 심볼릭 링크가 실제로 있거나 경로 상태를
안전하게 확인할 수 없으면 평가 삭제를 409로 막는다. 파일이 이미 없을 때만 평가와
연관 데이터베이스 행을 연쇄 삭제한다. 이 판정으로 정상 순서에서는 새 고아 파일을
만들지 않지만, 경로 확인 직후 다른 과정이 같은 위치에 파일을 만드는 매우 짧은
경쟁 구간은 남는다. 작업 12 이후 전용 저장 서비스가 파일 정리와 데이터베이스
삭제를 하나의 보상 가능한 흐름으로 묶어야 완전히 닫힌다.

## 검사 결과

- 구현 전 `.venv/bin/pytest tests/test_api_assessments.py -v`: 스물네 건 실패,
  평가 경로 미등록 404 확인.
- `.venv/bin/pytest tests/test_api_assessments.py -q`: 스물네 건 통과, 기존 외부
  도구 경고 두 건.
- `.venv/bin/pytest tests/ -q`: 이백삼십삼 건 통과, 기존 외부 도구 경고
  일곱 건.
- `.venv/bin/python -m compileall -q app tests`: 통과.
- `git diff --check`: 통과.
- `git diff --cached --check`: 통과.
- 고침 구현 전 초점 검사: 백마흔두 건 가운데 마흔여섯 건 예상 실패. 세 문제의
  느슨한 변환과 열린 삭제를 확인했다.
- 고침 뒤 초점 검사: 백마흔두 건 통과, 기존 외부 도구 경고 일곱 건.
- 고침 뒤 전체 백엔드 검사: 이백팔십육 건 통과, 기존 외부 도구 경고 일곱 건.
- 고침 뒤 `.venv/bin/python -m compileall -q app tests`: 통과.
- 고침 뒤 `git diff --check`와 `git diff --cached --check`: 통과.

## 남은 위험

- 실제 업로드 파일이 연결된 평가는 현재 삭제할 수 없다. 작업 12 저장 서비스와
  파일 정리 계약이 생긴 뒤 삭제 흐름을 그 서비스로 옮겨야 한다.
- 로컬 파일 존재 확인과 데이터베이스 삭제 사이의 짧은 경쟁 구간은 단일 사용자
  로컬 실행에서는 가능성이 낮지만 원자적이지 않다.
- 외부 의존성의 사용 중단 예정 경고 일곱 건이 남아 있다. 이번 변경에서 새로 만든
  경고는 아니며 검사 결과에는 영향을 주지 않는다.
